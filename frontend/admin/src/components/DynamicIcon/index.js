import * as AntIcons from '@ant-design/icons';

export const iconMap = AntIcons;

// export const DynamicIcon = ({ icon }) => {
//   if (!icon) return "";
//   const AntIcon = AntIcons[icon];
//   return <AntIcon />
// }